/**
 * @multiplist/mcp-server — public cloud proxy entry point.
 *
 * Compiled as a bin script; no runtime exports. Importing this module
 * starts the stdio ↔ HTTPS bridge immediately.
 */
export {};
